# AWS deployment notes (situational)

Only read/use this if the user explicitly asks about deploying/running
Szurubooru on AWS.

Primary references live in:

- `doc/aws-deployment-instructions/AWS_SETUP_AND_DEPLOYMENT.md`
- `doc/aws-deployment-instructions/APPENDIX_HTTPS.md`
- `doc/aws-deployment-instructions/APPENDIX_BACKUPS.md`
- `doc/aws-deployment-instructions/APPENDIX_CLOUDWATCH_LOGS.md`

As an AI agent:
- Prefer pointing the user to the repo’s existing AWS docs rather than
  inventing new infrastructure patterns.
- Make any changes to deployment configuration narrowly-scoped and
  reversible (infrastructure mistakes are high impact).

